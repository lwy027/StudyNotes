import { Person } from './dto/Person';
export declare class PipeAllController {
    constructor();
    dd(age: number): number;
    bb(bb: string): string;
    getHello(age: number): number;
    cc(kk: string): string;
    login(person: Person): Person;
}
